# Legacy file
#
# This file used to contain all personal overrides. The active configuration is
# now split across conf.d/*.conf and included directly from kitty.conf.
#
# Keep this file around as a note for muscle memory; it is not included by
# kitty.conf anymore.
